return {
    {
        "OXY2DEV/markview.nvim",
        lazy = false,
        priority = 49,
        opts = {
            -- experimental = {
            -- check_rtp = true,
            -- check_rtp_message = false
            -- }
        },
       -- For `nvim-treesitter` users

        -- dependencies = {
            -- "saghen/blink.cmp"
        -- }
    },
    -- {
        -- 'jghauser/follow-md-links.nvim',
        -- ft = "markdown",
    -- }
}
